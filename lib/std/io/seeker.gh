import "../result.gh" as result;

pub const Whence := enum { start, current, end };

pub const Seeker := interface {
    Error: type;

    /// Seeks to `offset` by `whence`
    pub const seek := fn(&mut self, offset: i64, whence: Whence): result::Result(i64, Error);
};
