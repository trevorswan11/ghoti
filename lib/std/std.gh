pub const version := "0.4.0-alpha.1";

import "types/result.gh" as result;
import "types/option.gh" as option;

pub using Result = result.Result;
pub using Option = option.Option;

pub import "os/os.gh" as os;
pub import "io/io.gh" as io;
pub import "testing/testing.gh" as testing;
pub import "mem/mem.gh" as mem;
pub import "heap/heap.gh" as heap;
pub import "fmt/fmt.gh" as fmt;
pub import "math/math.gh" as math;

test {
    import "os/os.gh";
    import "io/io.gh";
    import "mem/mem.gh";
    import "heap/heap.gh";
    import "fmt/fmt.gh";
    import "math/math.gh";
    import "types/result.gh";
    import "types/option.gh";
}

pub const panic_handler := fn(msg: []u8, file: []u8, _: u32, _: u32): noreturn {
    _ = io.eprint(file);
    _ = io.eprint(": ");
    _ = io.eprintln(msg);
    @trap();
};

pub const assert_handler := fn(msg: []u8, file: []u8, _: u32, _: u32): void {
    _ = io.eprint(file);
    _ = io.eprint(": ");
    _ = io.eprintln(msg);
    @trap();
};
